def task_3(intervals):
    intervals.sort(key=lambda x: x[0])
    # сортируем последовательность, чтобы не перебирать, для того, чтобы последовательности которые персекаются стояли рядом
    # когда задаем key мы показываем по какому значению мы сортируем


    i = 0 #инициализирует индекс i значением 0. Этот индекс используется для перебора отсортированных интервалов.

    while i + 1 < len(intervals):
        while intervals[i][1] >= intervals[i + 1][0]:  #цикл, пока они не пересекаются
            intervals[i][1] = max(intervals[i][1], intervals[i + 1][1])
            # если пересеклись, то правый элемент первого меняем на максимальный
            intervals.pop(i + 1)
            # удаляем второй элемент

            if i + 1 == len(intervals):
                break

        i += 1

    return intervals #После обработки всех интервалов, функция возвращает измененный список intervals, содержащий объединенные неперекрывающиеся интервалы.

