def task_4(s):
    max = 0 #Это переменная, которая будет хранить максимальную длину подстроки без повторяющихся символов.

    for i in range(len(s)): #перебирает все возможные стартовые позиции подстрок.
        repeat=[] #масив для проверки повторений
        for j in range(i, len(s)):

            #если мы нашли элемент второй раз выходим из цикла
            if s[j] in repeat:
                break #прерывает внутренний цикл, так как найдено повторение.

            else:
                #Если символ s[j] не найден в prom (то есть это новый уникальный символ), max обновляется, если длина текущей подстроки больше текущего максимального значения.
                max = maxx(max, j - i + 1)

                #добавляем элемент
                repeat.append(s[j])

    return max #После проверки всех возможных подстрок, функция возвращает значение max, представляющее длину самой длинной подстроки без повторяющихся символов.

