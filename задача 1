def recursion_task_1(n):
    if not n:
        return 0

    return (n % 10) ** 2 + recursion_task_1(n // 10)
# Эта функция является рекурсивной и вычисляет сумму квадратов цифр числа n.

def task_1(n):
    prom = [] #пустой список для промежуточных значений
    while n > 1:
        if n in prom:
            return False
        # проверяем втречалось ли число, если встречалось, то это не счастливое число (образовался цикл), и мы возвращаем False

        prom.append(n) # если не встречалось то добавляем его

        n = recursion_task_1(n) # вызывается для вычисления следующего значения n.
    return True # Если цикл завершился, и n стало равно 1, функция возвращает True, указывая, что число "счастливое".





